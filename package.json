{
  "name": "boilerplate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.15.5",
    "mongodb": "^2.2.31",
    "mongoose": "^4.11.13",
    "prop-types": "^15.6.0",
    "react": "^15.6.1",
    "react-day-picker": "^6.1.1",
    "react-dom": "^15.6.1",
    "react-scripts": "^1.0.13"
  },
  "scripts": {
    "//start": "react-scripts start",
    "start": "node api/server.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git add . && git commit -m 'new build' && git push && git push heroku master"
  },
  "proxy": "http://localhost:5000"
}
